{% extends 'base.html.twig' %}

{% block title %}OGRX{% endblock %}

{% block body %}
    <div class="div container">
        <div class="row">
            <a href="{{ path('show.users.all' ) }}" class="btn btn-primary">Retour à la liste</a>
        </div>

    </div>

    <div class="container mt-4"><h2>Utilisateurs</h2></div>

    <div class="div container">

        <table class="table table-striped">
            <thead>
            <tr>
                <td>Nom</td>
                <td>Prénom</td>
                <td>Email</td>
                <td>&nbsp;</td>

            </tr>
            </thead>
            <tbody>

                <tr>
                    <td>{{ user.name  }}</td>
                    <td>{{ user.firstname  }}</td>
                    <td>{{ user.email  }}&nbsp; &nbsp;</td>
                    <td><a href="}" ><img src="{{ asset('img/modif.png') }}" class="img-responsive" alt="Responsive image"></a></td>

                </tr>


            </tbody>
        </table>

    </div>
{% endblock %}